name: Build release artifacts

on:
  workflow_dispatch: {}
  release:
    types: [published]

env:
  configuration: Release
  path: src/ProjectOrigin.HierarchicalDeterministicKeys

jobs:
  define-version:
    uses: project-origin/.github/.github/workflows/reusable-tag-version.yaml@1c2d92ef17899775d5a5a7e9e91025e1d988049f

  build-nuget-package:
    needs:
      - define-version
    uses: project-origin/.github/.github/workflows/reusable-build-nuget-package.yaml@1c2d92ef17899775d5a5a7e9e91025e1d988049f
    with:
      version: ${{ needs.define-version.outputs.version }}
      configuration: Release
      nuget_artifact: nuget-packages
      project: .

  publish-nuget-package:
    needs:
      - build-nuget-package
    uses: project-origin/.github/.github/workflows/reusable-publish-nuget-artifacts.yaml@1c2d92ef17899775d5a5a7e9e91025e1d988049f
    with:
      nuget_artifact: nuget-packages
    secrets:
      nuget_api_key: ${{ secrets.NUGET_API_KEY }}
