name: Build release artifacts

on:
  workflow_dispatch: {}
  release:
    types: [published]

env:
  configuration: Release
  path: src/ProjectOrigin.HierarchicalDeterministicKeys

jobs:
  define-version:
    uses: project-origin/.github/.github/workflows/reusable-tag-version.yaml@a1a7a739e98b68d9b183af7ad7472b2ece460747

  build-nuget-package:
    needs:
      - define-version
    uses: project-origin/.github/.github/workflows/reusable-build-nuget-package.yaml@a1a7a739e98b68d9b183af7ad7472b2ece460747
    with:
      version: ${{ needs.define-version.outputs.version }}
      configuration: Release
      nuget_artifact: nuget-packages
      project: .

  publish-nuget-package:
    needs:
      - build-nuget-package
    uses: project-origin/.github/.github/workflows/reusable-publish-nuget-artifacts.yaml@a1a7a739e98b68d9b183af7ad7472b2ece460747
    with:
      nuget_artifact: nuget-packages
    secrets:
      nuget_api_key: ${{ secrets.NUGET_API_KEY }}
